
describe("convertPriceHandler", () => {

  it("returns 400 when invoked without contract", () => {
    
  });

  it("returns 400 when invoked without chain", () => {
    
  });

  it("returns price for valid requet", () => {

  });

  it("returns 500 when call to coingecko fails", () => {

  });
});
