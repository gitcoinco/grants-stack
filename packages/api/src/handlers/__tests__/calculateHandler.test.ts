
describe("calculateHandler", () => {

  it("returns 400 when invoked without chainId", () => {
    
  });

  it("returns 400 when invoked without roundId", () => {
    
  });

  it("returns 400 when invoked with a chainID which is not supported", () => {

  });

  it("ensures upsert for round table when parameters are valid", () => {

  });

  it("returns 400 if strategy name is not supported", () => {

  });

  it("ensures upsert for payout table when parameters are valid", () => {

  });

  it("returns valid response for valid parameters", () => {

  });
  
});
