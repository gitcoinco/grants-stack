{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "build/**"
      ],
      "outputLogs": "errors-only"
    },
    "test": {
      "inputs": [
        "src/**/*.tsx",
        "src/**/*.ts"
      ],
      "outputLogs": "errors-only"
    },
    "lint:ci": {
      "outputLogs": "errors-only"
    },
    "typecheck": {
      "inputs": [
        "src/**/*.tsx",
        "src/**/*.ts"
      ],
      "outputLogs": "errors-only"
    }
  }
}
